# dev-start-wp-kit

```
Commands to start:

make build
make install
make start


#fix if yarn send some error
echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf && sudo sysctl -p
```
